# ============================================
# Calendar Mod 资源包生成器 - 依赖清单
# ============================================
#
# 安装方式：
#   pip install -r requirements.txt
#
# 或单独安装：
#   pip install PySide6>=6.6.0
#
# ============================================
# 【第三方 pip 库 - 需安装】
# ============================================

# GUI 框架：Qt 6 官方 Python 绑定
# 用途：构建现代化 GUI 界面，提供窗口、按钮、表单、颜色选择器等
# 官网：https://www.qt.io/qt-for-python
PySide6>=6.6.0

# 颜色处理库
# 用途：颜色格式转换（HEX/RGB/HSL）、颜色调色板生成、颜色混合
# 官网：https://pypi.org/project/colour/
colour>=0.1.5

# 现代终端输出（可选，用于 CLI 模式的彩色日志）
# 用途：在终端模式下提供彩色高亮和格式化输出
# 官网：https://pypi.org/project/rich/
rich>=13.0.0

# ============================================
# 【Python 标准库 - 无需安装】
# ============================================
#   json      - JSON 序列化与反序列化
#   os        - 文件系统操作、路径处理
#   shutil    - 文件/目录复制与移动
#   pathlib   - 面向对象的路径处理
#   re        - 正则表达式（颜色格式校验）
#   sys       - 系统接口
#   datetime  - 时间戳生成
#   zipfile   - 资源包压缩导出
#   textwrap  - 文本格式化
#   typing    - 类型注解
